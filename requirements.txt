# Core dependencies (minimal)
torch>=2.2.0
einops>=0.7.0
opencv-python==4.10.0.82

# Development dependencies
jupyter>=1.0.0
black>=23.0.0
ruff>=0.0.280
torchmetrics>=0.11.0

# Optional for enhanced performance
# triton>=2.1.0  # For custom CUDA kernels (NVIDIA GPUs only)
# flash-attn>=2.0.0  # For Flash Attention (if available)